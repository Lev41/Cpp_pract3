#include <iostream>
using namespace std;

int main()
{
	setlocale(LC_ALL, "rus");
    int y;
    cin >> y;

    if (((y % 4 != 0) || (y % 100 == 0)) && (y % 400 != 0))
        cout << "Îáû÷íûé";
    else
        cout << "Âèñîêîñíûé";

	return 0;
}
